{
  "name": "tooltip",
  "type": "component",
  "description": "Hover/focus tooltip with side positioning.",
  "category": "Overlays",
  "dependencies": {
    "components": [],
    "js": [],
    "composer": ["gehrisandro/tailwind-merge-laravel"]
  },
  "files": [
    { "path": "tooltip.blade.php", "source": "files/tooltip.blade.php" }
  ],
  "tailwind": {
    "cssVars": ["--primary", "--primary-foreground", "--radius"],
    "notes": "Pure CSS (hover plus focus-within), no JavaScript at all. Wrap the trigger in the slot; pass `text` and optional `side` (top|bottom|left|right)."
  }
}
