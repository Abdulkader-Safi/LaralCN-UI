{
  "name": "dialog",
  "type": "component",
  "description": "Modal dialog with focus trap, backdrop, and Esc-to-close.",
  "category": "Overlays",
  "dependencies": {
    "components": ["button"],
    "js": [],
    "composer": ["gehrisandro/tailwind-merge-laravel"]
  },
  "files": [
    {
      "path": "dialog.blade.php",
      "source": "files/dialog.blade.php"
    }
  ],
  "tailwind": {
    "cssVars": [
      "--background",
      "--border",
      "--muted-foreground",
      "--ring",
      "--radius"
    ],
    "notes": "Built on the native <dialog> element (top layer, focus trap, Esc to close) plus a small inline <script>; no JS dependencies. The `trigger` slot opens the dialog; an optional `footer` slot renders action buttons."
  }
}
