{
  "name": "accordion",
  "type": "component",
  "description": "Collapsible disclosure list (single or multiple open).",
  "category": "Navigation",
  "dependencies": {
    "components": [],
    "js": [],
    "composer": ["gehrisandro/tailwind-merge-laravel"]
  },
  "files": [
    { "path": "accordion.blade.php", "source": "files/accordion.blade.php" }
  ],
  "tailwind": {
    "cssVars": ["--border", "--muted-foreground", "--ring", "--radius"],
    "notes": "Ships its own inline <script>; no JS dependencies. Pass `:items` as ['key' => 'Label']; provide each body via a named slot `item_<key>`. Set `multiple` to allow several open."
  }
}
